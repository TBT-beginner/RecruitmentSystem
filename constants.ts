
import { SchoolData } from './types';

// 定数はスプレッドシート管理に移行しました。
// 初期値が必要な場合のフォールバックデータ定義
export const DEFAULT_RANKS = ['S1', 'S2', 'S3', 'S4', 'なし'];
export const DEFAULT_RESULTS = ['未定', '確約/合格', '辞退', '保留'];
export const DEFAULT_PROSPECTS = ['○', '×', '未定'];
